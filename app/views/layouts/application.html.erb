<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Csc Ece Spring 2026 Program 2" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="site-header">
      <h1 class="site-title">Volunteer Management System</h1>
      <nav class="site-nav">
        <%= link_to "Home", root_path %>
        <% if volunteer_signed_in? %>
          <%= link_to "Dashboard", volunteer_dashboard_path %>
          <%= link_to "Events", events_path %>
          <%= link_to "Assignments", volunteer_assignments_path %>
          <%= link_to "History", volunteer_history_path %>
          <%= button_to "Logout", volunteer_logout_path, method: :delete, class: "link-button" %>
        <% elsif admin_signed_in? %>
          <%= link_to "Dashboard", admin_dashboard_path %>
          <%= link_to "Volunteers", admin_volunteers_path %>
          <%= link_to "Events", admin_events_path %>
          <%= link_to "Assignments", admin_volunteer_assignments_path %>
          <%= link_to "Analytics", admin_analytics_path %>
          <%= button_to "Logout", admin_logout_path, method: :delete, class: "link-button" %>
        <% else %>
          <%= link_to "Volunteer Sign Up", volunteer_signup_path %>
          <%= link_to "Volunteer Login", volunteer_login_path %>
          <%= link_to "Admin Login", admin_login_path %>
        <% end %>
      </nav>
    </header>

    <main class="site-main">
      <% flash.each do |type, message| %>
        <div class="flash <%= type %>"><%= message %></div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
